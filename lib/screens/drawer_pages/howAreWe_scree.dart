import 'package:flutter/material.dart';

class HowAreWeScreen extends StatelessWidget {
  const HowAreWeScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('How Are We Screen'),),
    );
  }
}